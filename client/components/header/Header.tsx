import React, { FC } from 'react';
import Search from './Search';

const Header: FC = () => {
    return (
        <>
            <div>
                    <Search />

            </div>

        </>
    );
};

export default Header;